import { AuthData } from '@lit-protocol/schemas';
import { AuthMethod, AuthSig, BaseSiweMessage } from '@lit-protocol/types';
import { GetWalletClientReturnType } from '@wagmi/core';
import { Hex, WalletClient } from 'viem';
export type WalletClientAuthenticateOverrides = Partial<Omit<BaseSiweMessage, 'walletAddress' | 'nonce'>>;
export declare class WalletClientAuthenticator {
    readonly type = "walletClient";
    static createAuthSig(account: GetWalletClientReturnType | WalletClient, messageToSign: string): Promise<AuthSig>;
    /**
     * Generate an AuthSig for the connected wallet. Provide a full message to sign via `messageToSign`,
     * or let the helper build one while overriding specific SIWE fields with `siweMessageOverrides`.
     */
    static authenticate(account: GetWalletClientReturnType | WalletClient, messageToSign?: string, siweMessageOverrides?: WalletClientAuthenticateOverrides): Promise<AuthData>;
    static authMethodId(authMethod: AuthMethod): Promise<Hex>;
}
