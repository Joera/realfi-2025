"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.undelegatePaymentsBatch = undelegatePaymentsBatch;
const zod_1 = require("zod");
const logger_1 = require("../../../../../../../../shared/logger");
const createContractsManager_1 = require("../../../../../contract-manager/createContractsManager");
const callWithAdjustedOverrides_1 = require("../../../utils/callWithAdjustedOverrides");
const decodeLogs_1 = require("../../../utils/decodeLogs");
// Schema for validating the request
const undelegatePaymentsBatchSchema = zod_1.z.object({
    userAddresses: zod_1.z.array(zod_1.z.string().regex(/^0x[a-fA-F0-9]{40}$/, 'Invalid Ethereum address')),
});
/**
 * Undelegate payments from multiple users in batch
 * @param request - Object containing array of userAddresses
 * @param networkCtx - Network context for contract interactions
 * @param accountOrWalletClient - Account or wallet client for the transaction
 * @returns Transaction result with hash, receipt and decoded logs
 */
async function undelegatePaymentsBatch(request, networkCtx, accountOrWalletClient) {
    const validatedRequest = undelegatePaymentsBatchSchema.parse(request);
    logger_1.logger.debug({ validatedRequest });
    const { paymentDelegationContract, publicClient } = (0, createContractsManager_1.createContractsManager)(networkCtx, accountOrWalletClient);
    const hash = await (0, callWithAdjustedOverrides_1.callWithAdjustedOverrides)(paymentDelegationContract, 'undelegatePaymentsBatch', [validatedRequest.userAddresses]);
    const receipt = await publicClient.waitForTransactionReceipt({ hash });
    const decodedLogs = await (0, decodeLogs_1.decodeLogs)(receipt.logs, networkCtx, accountOrWalletClient);
    return { hash, receipt, decodedLogs };
}
//# sourceMappingURL=undelegatePaymentsBatch.js.map