"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRestriction = getRestriction;
const zod_1 = require("zod");
const logger_1 = require("../../../../../../../../shared/logger");
const createContractsManager_1 = require("../../../../../contract-manager/createContractsManager");
// Schema for validating the request
const getRestrictionSchema = zod_1.z.object({
    payerAddress: zod_1.z
        .string()
        .regex(/^0x[a-fA-F0-9]{40}$/, 'Invalid Ethereum address'),
});
/**
 * Get restriction for a payer from the PaymentDelegation contract
 * @param request - Object containing payerAddress
 * @param networkCtx - Network context for contract interactions
 * @param accountOrWalletClient - Account or wallet client for the transaction
 * @returns Restriction object
 */
async function getRestriction(request, networkCtx, accountOrWalletClient) {
    const validatedRequest = getRestrictionSchema.parse(request);
    logger_1.logger.debug({ validatedRequest });
    const { paymentDelegationContract } = (0, createContractsManager_1.createContractsManager)(networkCtx, accountOrWalletClient);
    const restriction = await paymentDelegationContract.read.getRestriction([
        validatedRequest.payerAddress,
    ]);
    return restriction;
}
//# sourceMappingURL=getRestriction.js.map