import type { LitAuthStorageProvider } from '../../storage';
export declare function tryGetCachedDelegationAuthSig(params: {
    storage: LitAuthStorageProvider;
    address: string;
    expiration: string;
    signSessionKey: any;
    cache?: boolean;
}): Promise<any>;
