"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPayersAndRestrictions = getPayersAndRestrictions;
const zod_1 = require("zod");
const logger_1 = require("../../../../../../../../shared/logger");
const createContractsManager_1 = require("../../../../../contract-manager/createContractsManager");
// Schema for validating the request
const getPayersAndRestrictionsSchema = zod_1.z.object({
    userAddresses: zod_1.z.array(zod_1.z.string().regex(/^0x[a-fA-F0-9]{40}$/, 'Invalid Ethereum address')),
});
/**
 * Get payers and restrictions for multiple users from the PaymentDelegation contract
 * @param request - Object containing array of userAddresses
 * @param networkCtx - Network context for contract interactions
 * @param accountOrWalletClient - Account or wallet client for the transaction
 * @returns Object containing arrays of payers and restrictions
 */
async function getPayersAndRestrictions(request, networkCtx, accountOrWalletClient) {
    const validatedRequest = getPayersAndRestrictionsSchema.parse(request);
    logger_1.logger.debug({ validatedRequest });
    const { paymentDelegationContract } = (0, createContractsManager_1.createContractsManager)(networkCtx, accountOrWalletClient);
    const [payers, restrictions] = await paymentDelegationContract.read.getPayersAndRestrictions([
        validatedRequest.userAddresses,
    ]);
    return { payers, restrictions };
}
//# sourceMappingURL=getPayersAndRestrictions.js.map