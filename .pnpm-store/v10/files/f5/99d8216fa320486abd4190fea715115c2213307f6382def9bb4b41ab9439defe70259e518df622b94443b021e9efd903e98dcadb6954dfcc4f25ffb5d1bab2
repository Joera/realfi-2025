"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.processResources = void 0;
const auth_helpers_1 = require("@lit-protocol/auth-helpers");
const processResources = (resources) => {
    let processedResources;
    // Transform resources if they are in shorthand format
    if ((0, auth_helpers_1.isResourceShorthandInput)(resources)) {
        processedResources = (0, auth_helpers_1.transformShorthandResources)(resources);
    }
    else {
        // Type assertion: Assuming if not shorthand, it's already the correct full format.
        processedResources = resources;
    }
    return processedResources;
};
exports.processResources = processResources;
//# sourceMappingURL=processResources.js.map